{
  "name": "traversy-node-masterclass",
  "version": "1.0.0",
  "description": "devcamper backend ap",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set NODE_ENV=production && node server",
    "dev": "nodemon server"
  },
  "author": "wly185",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
